<div class="container d-flex justify-content-center">
  <div>
    <h1 class="mb-4">All restaurants</h1>
    <ul class="list-group">
      <% @restaurants.each do |restaurant| %>
        <li class="list-group-item"> -> <%= link_to restaurant.name, restaurant_path(restaurant), class: "no-underline" %></li>
      <% end %>
    </ul>
  </div>
</div>
